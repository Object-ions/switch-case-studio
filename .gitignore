# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.js

# testing
/coverage

# production
/build

# misc
.env
.DS_Store
.env.local
.env.development.local
.env.test.local
.env.production.local

npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Graphify knowledge-graph output — regenerable tooling artifact, kept local only
graphify-out/
.netlify

# Illustrator source files (kept locally, not in repo)
*.ai

# Private strategy docs — candid self-assessment, never commit (public repo)
switch-case-studio-action-plan.md
switch-case-studio-objective-audit.md
phase-3-session-prompt.md

# Font sources — the unsubsetted brand OTF stays local; only the subset woff2 in public/ ships
fonts-src/

# Local AI agent skills / tooling (Higgsfield CLI skills install) — do not ship in public repo
.agents/
.claude/
skills-lock.json

# Case-study measurement workspaces — internal only, and this repo is public.
# Raw Lighthouse runs, full-page captures of a client's pre-rebuild site and the
# scripts behind them are evidence, not portfolio. Only the projects.json entry
# and the webp set under public/projects/ ever ship.
casestudy/
**/casestudy/
internal/
lighthouse-*/
lh/
*-medians.json
metrics.csv
METHOD.md
